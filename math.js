/**
 * Function to get the sqrt root of a number
 * @param {Integer} a
 * @returns
 */
const sqrt = (a) => {
  return Math.sqrt(a);
};

/**
 * This function sums twp numbers
 * @param {Integer} a
 * @param {Integer} b
 * @returns
 */
function sum(a, b) {
  return a + b;
}
