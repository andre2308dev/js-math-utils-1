/**
 * Function to get the sqrt root of a number
 * @param {Integer} a
 * @returns
 */
const sqrt = (a) => {
  return Math.sqrt(a);
};

/**
 * Function multiplication
 * @param {Number} a 
 * @param {Number} b 
 */
 const multiplication = (a,b) => {
  console.log(a*b);
}
